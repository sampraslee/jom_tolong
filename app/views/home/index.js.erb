var user_partial = document.getElementById('users-partial');

user_partial.innerHTML = "<%= j (render partial: 'users') %>";

var swiper1 = new Swiper('.swiper1', {
	direction: 'horizontal',
	loop: 'true',
	navigation: {
		nextEl: '.swiper-button-next',
		prevEl: '.swiper-button-prev'
	}
});
